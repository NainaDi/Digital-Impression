mutation ProductUpdateBasic($product: ProductUpdateInput!) {
  productUpdate(product: $product) {
    product {
      id
      title
      handle
      descriptionHtml
      status
      vendor
      productType
      updatedAt
    }
    userErrors { field message }
  }
}
